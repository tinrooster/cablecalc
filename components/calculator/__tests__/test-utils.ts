import { CalculatorSettings } from '@/types/calculator'

export const mockDefaultSettings: CalculatorSettings = {
  // Copy your working default settings here
}

export const mockCalculation = {
  // Copy your working calculation mock here
} 